# PostgreSQL connection used by the API and tests.
# Default matches docker-compose.yml
DATABASE_URL=postgresql+psycopg://tasks:tasks@localhost:5433/tasks

APP_TITLE=Task Management API
APP_VERSION=2.0.0
