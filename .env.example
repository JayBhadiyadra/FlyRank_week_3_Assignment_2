# Local PostgreSQL connection (Assignment 2 — no Docker).
# Create the database first (see scripts/setup_postgres.sql), then copy to .env.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/tasks
